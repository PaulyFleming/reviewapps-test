{
    "env": {
        "SECRET_TOKEN": "prod"
    },
    "environments": {
        "review": {
            "env": {
                "TEST_VAR":"review-test"
            }
        },
        "addons" : [
        {
            "plan": "heroku-postgresql:standard-0",
        "options": {
            "extensions": "pg_stat_statements, plpgsql, uuid-ossp"
        }
        } 
    ]
    },
    "addons" : [
        {
            "plan": "heroku-postgresql:standard-2",
        "options": {
            "extensions": "pg_stat_statements, plpgsql, uuid-ossp"
        }
        } 
    ]
}

